# SNO Quadro — Поиск фото товаров из Excel

Веб-приложение, которое читает Excel-файл прихода (.xls / .xlsx), парсит названия товаров (бренд, модель, цвет) и автоматически ищет фото через **Яндекс.Картинки** с помощью Selenium.

## Возможности

- **Парсинг Excel** — автоматическое определение заголовков и столбца «Товар»
- **Поиск фото** — Яндекс.Картинки (headless Chrome, большие изображения)
- **Выбор фото** — веб-интерфейс с превью, ссылки на источники
- **Удаление фона** — AI (rembg / ONNX) → белый фон
- **Ресайз и сохранение** — произвольный размер, JPEG 95%

Тёмный интерфейс в стиле X.com с карточками изображений, индикатором прогресса и панелью сохранения.

---

## Локальная установка

### 1. Требования

- **Python 3.10+**
- **Google Chrome** (или Chromium) — Selenium Manager скачает нужную версию автоматически
- **pip**

### 2. Клонирование

```bash
git clone https://github.com/GG1KENOBI/sno_quadro.git
cd sno_quadro
```

### 3. Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### 4. Установка Chrome (если нет)

**Ubuntu / Debian:**
```bash
# Selenium Manager скачает Chrome автоматически при первом запуске.
# Но нужны системные библиотеки:
sudo apt-get update && sudo apt-get install -y \
  libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 \
  libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
  libgbm1 libpango-1.0-0 libcairo2 libasound2t64 \
  libnspr4 libnss3 libxkbcommon0 fonts-liberation
```

**macOS:**
```bash
brew install --cask google-chrome
```

**Windows:**
Скачайте Chrome с https://www.google.com/chrome/

### 5. Запуск

```bash
python app.py
```

Откройте **http://localhost:5000** в браузере.

---

## Использование (веб-интерфейс)

1. Откройте http://localhost:5000
2. Перетащите Excel-файл (.xls / .xlsx) в область загрузки
3. Нажмите **«Искать все фото»** или кнопку **«Искать»** у конкретного товара
4. Кликните по карточке фото для выбора (синяя рамка = выбрано)
5. Нажмите кнопку 🎨 на карточке для удаления фона (AI)
6. Задайте размер (ширина × высота) в нижней панели, или оставьте 0 для оригинала
7. Нажмите **«Сохранить»** — файлы появятся в папке `output/<session_id>/`

Каждая карточка показывает:
- Размер изображения в пикселях
- Ссылку на сайт-источник (кликабельная)

---

## CLI-режим

```bash
# Скачать по 1 фото на товар
python search_images.py приход6.xls

# 3 фото на товар, в папку ./фото
python search_images.py приход6.xls --output ./фото --count 3

# Только показать товары (без скачивания)
python search_images.py приход6.xls --dry-run
```

### Параметры CLI

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--output`, `-o` | Папка для сохранения фото | `images` |
| `--count`, `-c` | Количество фото на товар | `1` |
| `--delay`, `-d` | Задержка между запросами (сек) | `1.0` |
| `--dry-run` | Показать товары без скачивания | — |

---

## Формат входного файла

Excel-файл со столбцом **«Товар»** (или «Наименование»):

| № | Товар | Количество | Цена |
|---|-------|------------|------|
| 1 | Chopard 358V 700 м/о | 2 | 15000 |
| 2 | Chopard 402S 700 м/о | 1 | 18000 |

Программа автоматически:
- Ищет строку-заголовок (не обязательно первая строка)
- Парсит «Бренд Модель Цвет» из названия
- Убирает суффиксы (м/о, ж/о, унисекс)
- Дедуплицирует одинаковые товары

---

## Структура проекта

```
sno_quadro/
├── app.py              # Flask веб-приложение
├── search_images.py    # CLI + модуль поиска (Selenium, парсинг Excel)
├── requirements.txt    # Python зависимости
├── templates/
│   └── index.html      # Веб-интерфейс (тёмная тема)
├── uploads/            # Загруженные Excel (gitignored)
├── temp_images/        # Временные изображения (gitignored)
├── output/             # Сохранённые результаты (gitignored)
└── images/             # CLI-результаты (gitignored)
```

## Зависимости

| Пакет | Назначение |
|-------|-----------|
| flask | Веб-сервер |
| selenium | Управление Chrome |
| xlrd | Чтение .xls |
| openpyxl | Чтение .xlsx |
| Pillow | Обработка изображений |
| rembg | Удаление фона (AI) |
| onnxruntime | ML-движок для rembg |
| requests | HTTP-запросы |
